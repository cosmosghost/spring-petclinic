# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql:petclinic-db.c3mymwogk8fv.ap-northeast-2.rds.amazonaws.com:3306/petclinic-db}
spring.datasource.username=${MYSQL_USER:admin}
spring.datasource.password=${MYSQL_PASS:adminm03d05}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
